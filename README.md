This folder holds published quiz JSON files, e.g. quiz_abc123.json.

Each file is created by the admin panel's "Save" or "Download File" button,
then must be manually placed here and pushed to GitHub for students to
access it (see README.md for the full publish workflow).
